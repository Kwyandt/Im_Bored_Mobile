package com.example.imbored.ui.gallery

class GalleryModel(val stringResourceId: Int, val imageResourceId: Int)
//TODO for implementation string resource ID and imageResourceID will need to be strings, not hard coded
